{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile"  // Try lowercase "dockerfile"
  },
  "deploy": {
    "startCommand": "hypercorn api:app --bind \"[::]:$PORT\""
  }
}
